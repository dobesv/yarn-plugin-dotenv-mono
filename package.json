{
  "name": "yarn-plugin-dotenv-mono",
  "version": "1.0.1",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dobesv/yarn-plugin-dotenv-mono"
  },
  "main": "index.ts",
  "files": [
    "index.ts",
    "bundles"
  ],
  "scripts": {
    "build": "builder build plugin && yarn plugin import ./bundles/@yarnpkg/plugin-dotenv-mono.js",
    "test": "echo run ./test.bash instead && false"
  },
  "dependencies": {
    "dotenv-mono": ">=1.3.10"
  },
  "devDependencies": {
    "@types/node": "^18.14.1",
    "@yarnpkg/builder": "^3.2.7",
    "@yarnpkg/core": "^3.4.0",
    "typescript": "^4.8"
  }
}
